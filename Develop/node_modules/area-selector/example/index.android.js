import React, { Component } from 'react';
import { AppRegistry,View, ListView, StyleSheet,Text } from 'react-native';
var Home = require('./Home')

AppRegistry.registerComponent('AreaSelector', () => Home);
